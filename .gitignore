# Binaries
/capy
/dist/
*.exe
*.test
*.out
.ignore
# OS junk
.DS_Store
Thumbs.db

# Editor configs
.idea/
.vscode/
*.swp
*~

# Local agent / tooling state (do NOT ship)
.claude/

# MkDocs build output (deployed by GitHub Actions)
/site/

# Playground build artifacts (built fresh by CI for every deploy;
# rebuild locally with `scripts/build-playground.sh`).
/docs/assets/playground/capy.wasm
/docs/assets/playground/wasm_exec.js
/docs/assets/playground/samples.json
commands.yaml

# Build output from the standalone example projects.
examples/*/target/
examples/*/Cargo.lock

# Gradle build cache, if someone runs gradle inside a sample expected/ tree.
.gradle/
