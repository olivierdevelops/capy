> **HISTORICAL.** Describes an earlier design, written when the engine was
> implemented in Go. The engine is now Rust under `rust/`. Kept for reference.

# Legacy / historical reference

Older documents kept for context, superseded by the current docs tree.

| File | What it is |
|---|---|
| `language-spec-original.md` | The original README that doubled as a language specification, pre-rewrite. |
| `vhco-architecture.md`     | The original `.codestyle` describing the VHCO project layout. The current canonical version lives at [`docs/architecture.md`](../architecture.md). |